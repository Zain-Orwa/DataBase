# 01 - Create Database

To create a new database in SQL Server:

```sql
CREATE DATABASE koko;
```

## What it does

This creates a new database named `koko`.

## Important

If a database named `koko` already exists, SQL Server will return an error because two databases cannot have the same name.
